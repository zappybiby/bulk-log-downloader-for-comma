{
  "name": "comma-bulk-log-downloader",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox Android prototype and synthetic tests for the comma bulk log downloader",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint:firefox": "web-ext lint --source-dir firefox --warnings-as-errors",
    "package:firefox": "python3 scripts/package-firefox.py"
  },
  "devDependencies": {
    "linkedom": "0.18.13",
    "web-ext": "10.6.0"
  }
}
